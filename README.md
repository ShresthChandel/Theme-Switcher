# Theme-Switcher
React application that allows users to toggle between light and dark themes, demonstrating theme management and global state handling in React.  The app focuses on React state management, Context API usage, and UI theming best practices without relying on any backend services.
